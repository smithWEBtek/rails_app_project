<% if @distillery.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@distillery.errors.count, "error") %>
      prohibited this distillery from being saved:
    </h2>

    <ul>
    <% @distillery.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>


<%= form_for @distillery  do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :address %>
<%= f.text_field :address %>

<br />

<%= f.submit %>
<% end %>
